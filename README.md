# OPENCLAW

- ERC-20 token contract (`OpenClaw`, symbol `OCL`) for Sepolia
- Minimal Web3 dApp served by GitHub Pages

## Status
- Contract: pending deployment (needs funded deployer wallet)
- dApp: in `docs/`

## Dev
- `npm install`
- `npm run compile`
- `npm run deploy:sepolia`
